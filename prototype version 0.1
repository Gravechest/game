import random
z1 = 0
z2 = 0
z3 = 0
y = 0
x = 0
enl = 0
aps = 0
ntclsd = 0
mapgen = []
opendoors = []
enemie = []
enemies = []
enemiel = []
while True:
    while z1 < 7:
        if z3 == 3:
            y = y + 2
            x = x + 3
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if (y+(x*100)) in opendoors:
                opendoors.remove(y+(x*100))
            elif atrib < 80:
                opendoors.append(y+(x*100))
            y = y + 1
            x = x - 1
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if (y+(x*100)) in opendoors:
                opendoors.remove(y+(x*100))
            elif atrib < 80:
                opendoors.append(y+(x*100))
            y = y + 1
            x = x + 1
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if (y+(x*100)) in opendoors:
                opendoors.remove(y+(x*100))
            elif atrib < 80:
                opendoors.append(y+(x*100))
            y = y - 1
            x = x + 1
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if (y+(x*100)) in opendoors:
                opendoors.remove(y+(x*100))
            elif atrib < 80:
                opendoors.append(y+(x*100))
            y = y - 3
            x = x - 4
            z3 = 0
        if z2 == 0 and z3 == 0:
            y = y + 3
            x = x + 3
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if rnd == ["#"]:
                z3 = 2
            elif (y+(x*100)) in opendoors:
                ntclsd = 1
                z3 = 1
            elif atrib < 80 and ntclsd == 0:
                z3 = 2
            else:
                z3 = 1
            y = y - 3
            x = x - 3
        if z3 == 1:
            random.seed(y+(x*100))
            rnd = random.choices((".","#"))
            atrib = random.randint(0,1000)
            if rnd == ["#"]:
                rnd = "#"
            elif atrib < 80:
                rnd = "C"
            elif (y+(x*100)) in enemie:
                enemie.remove(y+(x*100))
                enemies.append(y+(x*100))
                if z2 < 3:
                    enemiel.append(y+((x+1)*100))
                elif z2 > 2:
                    enemiel.append(y+((x-1)*100))
                rnd = "."
            elif (y+(x*100)) in enemiel:
                enemiel.remove(y+(x*100))
                enemie.append(y+(x*100))
                rnd = "E"
            elif atrib > 80 and atrib < 160 and (y+(x*100)) not in enemies:
                rnd = "E"
                enemie.append(y+(x*100))
            elif y+(x*100) in opendoors:
                rnd = "c"
            elif z2 == 3 and y == 3 + aps:
                rnd = "@"
            else:
                rnd = "."
            mapgen.append(rnd)
            y = y + 1
            if z1 == 6:
                x = x + 1
                y = y - 7
            z1 = z1 + 1
        if z1 == 7 and z2 < 7 and z3 == 1:
            print(*mapgen)
            z1 = 0
            mapgen.clear()
            z2 = z2 + 1
        if z2 == 7 and z3 == 1 or z3 == 2:
            if z3 != 2:
                x = x - 7
            else:
                if inp == "6":
                    y = y - 1
                elif inp == "2":
                    x = x - 1
                elif inp == "4":
                    y = y + 1
                else:
                    x = x + 1
                print("not a valid move")
            inp = input("action")
            if inp == "6":
                y = y + 1
                z2 = 0
            elif inp == "2":
                x = x + 1
                z2 = 0
            elif inp == "4":
                y = y - 1
                z2 = 0
            elif inp == "8":
                x = x - 1
                z2 = 0
            z3 = 0
            if inp == "o":
                z3 = 3
                z2 = 0
            aps = y
            ntclsd = 0
